import {Router} from "express"; import auth from "../middleware/auth"; import admin from "../middleware/admin"; import {ProductController} from "../controllers/product.controller"; const r=Router();r.get("/products",ProductController.publicList);r.get("/admin/products",auth,admin,ProductController.adminList);r.post("/admin/products",auth,admin,ProductController.create);r.put("/admin/products/:id",auth,admin,ProductController.update);r.delete("/admin/products/:id",auth,admin,ProductController.remove);export default r;