"use client"; import {useState} from "react";import {useRouter} from "next/navigation";import {api} from "@/lib/api";export default function Register(){const r=useRouter();const[e,setE]=useState("");const[p,setP]=useState("");return <main className="flex min-h-screen items-center justify-center px-6"><form className="glass w-full max-w-md rounded-3xl p-7" onSubmit={async x=>{x.preventDefault();try{await api.post("/auth/register",{email:e,password:p});r.push("/login")}catch{alert("Registrasi gagal")}}}><h1 className="text-3xl font-black">Daftar</h1><input className="mt-6 w-full rounded-xl bg-black/40 p-3" placeholder="Email" value={e} onChange={x=>setE(x.target.value)}/><input className="mt-4 w-full rounded-xl bg-black/40 p-3" placeholder="Password" type="password" value={p} onChange={x=>setP(x.target.value)}/><button className="mt-6 w-full rounded-xl bg-cyan-400 p-3 font-bold text-black">Daftar</button></form></main>}