"use client"; import {useEffect,useState} from "react";import Link from "next/link";import {api} from "@/lib/api";export default function Products(){const[p,setP]=useState<any[]>([]);useEffect(()=>{api.get("/products").then(x=>setP(x.data.data)).catch(()=>{})},[]);return <main className="mx-auto max-w-6xl px-6 pt-32"><h1 className="text-4xl font-black">Paket Panel</h1><div className="mt-8 grid gap-5 md:grid-cols-3">{p.map(x=><div className="glass rounded-3xl p-6" key={x.id}><h2 className="text-2xl font-bold">{x.name}</h2><p className="mt-4 text-slate-400">{x.ram}MB RAM • {x.disk}MB Disk • {x.cpu}% CPU</p><b className="mt-5 block text-2xl text-cyan-300">Rp {x.price.toLocaleString("id-ID")}</b><Link href={`/checkout/${x.id}`} className="mt-5 block rounded-xl bg-cyan-400 p-3 text-center font-bold text-black">Beli</Link></div>)}</div></main>}